<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">

      <!-- ✅ 修復點 1：這兩個 meta-data 維持在 application 層級（你原本就是對的） -->
      <meta-data android:name="androidx.health.PRIVACY_POLICY_URL" android:value="https://your-real-policy-url.com" />
      <meta-data android:name="health_permissions" android:resource="@array/health_permissions" />

      <!-- ✅ 修復點 2：ATTRIBUTION_TAG 必須在 application 層級，移出 activity -->
      <property
        android:name="android.health.READ_BLOOD_PRESSURE_ATTRIBUTION_TAG"
        android:value="BloodPressureSync" />
      <property
        android:name="android.health.READ_HEART_RATE_ATTRIBUTION_TAG"
        android:value="HeartRateSync" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <intent-filter>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent-filter>

        <!-- ✅ 修復點 3：加入這個 meta-data，告知套件使用哪個 Activity 初始化 launcher -->
        <!-- 這是修復 lateinit 崩潰的核心 -->
        <meta-data
          android:name="health_connect_client_activity"
          android:value=".MainActivity" />
      </activity>

      <activity-alias
          android:name=".ViewPermissionUsageActivity"
          android:exported="true"
          android:targetActivity=".MainActivity"
          android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
          <intent-filter>
              <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
              <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
          </intent-filter>
      </activity-alias>

    </application>
</manifest>
